<table align="center">
    <h2 style="text-align:center">Find Compare Value In SQL</h2>

    <h3 style="color:yellow" onclick="muaxuan()">@ViewBag.KetQua</h3>

   <h3>@Html.ActionLink("Xử Lý SQL 1 ? (tạo proc count all table/dành cho admin)","XuLySQL1")</h3>
   <h3>@Html.ActionLink("Xử Lý SQL 2 ? (tạo-xoá trigger thông báo phát hiện Table insert-update-delete/dành cho admin)","XuLySQL2")</h3>
   <h3>@Html.ActionLink("Xử Lý SQL 3 ? (tên cột #3275# old value #3275# new value - trình bày lại để có thể copy vào Excel/dành cho admin)","XuLySQL3")</h3>
   <h3>@Html.ActionLink("Xử Lý SQL 4 ? (tìm phát hiện và thêm cột của Table còn thiếu [đã biết trước]/dành cho admin)","XuLySQL4")</h3>
    <h3>@Html.ActionLink("Xử Lý SQL 5 ? (cũng như No.1 nhưng nhập các Tables bằng file)","XuLySQL5")</h3>
     <h3>@Html.ActionLink("Xử Lý SQL 6 ? (nhập file và tìm các Table còn thiếu)","XuLySQL6")</h3>
     <h3>@Html.ActionLink("Xử Lý SQL 7 ? (tìm phát hiện và thêm cột của Table còn thiếu [chưa biết trước & duyệt tất cả]/dành cho admin)","XuLySQL7")</h3>
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true)

        <tr style="height:40px">
            <td> </td>
            <td>
                <br>
                    <b>Chuỗi các fields của bạn (chú ý index - cách nhau bởi Enter) : <br>
                    </b>
                     <br>
                    <textarea cols="50" rows="12" name="txtFields"></textarea>
            </td>
        </tr>

         <tr style="height:40px">
            <td> </td>
            <td>
                <br>
                    <b>Chuỗi các giá trị cũ của dữ liệu (chú ý phải theo index - tách nhau bởi Enter và Tab) <br>
                    </b>
                     <br>
                    <textarea cols="100" rows="12" name="txtOld"></textarea>
            </td>
        </tr>

        <tr style="height:40px">
            <td> </td>
            <td>
                <br>
                    <b>Chuỗi các giá trị mới của dữ liệu (chú ý phải theo index - tách nhau bởi Enter và Tab) <br>
                    </b>
                     <br>
                    <textarea cols="100" rows="12" name="txtNew"></textarea>
            </td>
        </tr>

         <tr style="height:40px">
            <td> </td>
            <td>
                <br>
                    <b>Nhập các fields làm dữ kiện nhận dạng (cách nhau bởi dấu phẩy) <br>
                    </b>
                     <br>
                    <input type="text" size="50" name="txtWhere" />
            </td>
        </tr>

        <tr style="height:40px">
            <td></td>
            <td><br>
                <input type="Submit" value="Kết Quả" />
            </td>
        </tr>
    }
</table>
<br /><br />

******************************************************************************************************************************************************************************

<br />
<br />
<br />

<div id="hihi">
                @Html.Raw(ViewBag.Result)
</div>
